can we recover with `y ~ s(x)`?
both modeling and wo modeling sigma

difficulties the covariate is functional

actually fit something where the mediator is not observed

what does the `set_rescor(FALSE)` thing do?

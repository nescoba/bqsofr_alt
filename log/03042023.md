Looking into modeling `y~s(x)` and `log(sigma) ~ s(x)`

The conclusion is that there is barely any difference between the two.
Both fit `y` well
The `sigma`s are not the same as the sd of the model

Is it possible to fit the parameters of GAL using the missing trick?
